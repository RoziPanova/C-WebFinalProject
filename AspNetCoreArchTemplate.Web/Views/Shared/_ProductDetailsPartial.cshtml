@using static AspNetCoreArchTemplate.GCommon.ApplicationConstants
@model dynamic
<div class="d-flex justify-content-start mb-3">
    <a asp-action="Index" class="btn btn-link text-decoration-none mb-3">
        <i class="bi bi-arrow-left"></i>
    </a>
</div>
<div class="col-md-6">
    <img src="@Model.ImageUrl" alt="@Model.Name" class="img-fluid rounded shadow-sm" />
</div>


<div class="col-md-6 d-flex flex-column justify-content-start">

    <h2 class="mb-3">@Model.Name</h2>
    <p class="text-muted mb-4">@Model.Description</p>
    @if (this.Model.EventType != null)
    {
        <p class="text-muted mb-4">For Events like: @Model.EventType</p>
    }
    <h4 class="text-primary mb-4">$@Model.Price.ToString(PriceFormat)</h4>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success" role="alert">
            @TempData["SuccessMessage"]
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger" role="alert">
            @TempData["ErrorMessage"]
        </div>
    }
    <form asp-controller="Cart" asp-action="Add" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" name="productId" value="@Model.Id" />
        <button type="submit" class="btn btn-success">Add to Cart</button>
    </form>
</div>